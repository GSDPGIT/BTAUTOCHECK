# 🤖 AI安全分析配置指南

BTAUTOCHECK V2.0 支持9种主流AI模型进行深度安全分析，您可以根据需要选择合适的AI提供商。

---

## 📋 支持的AI模型

| AI模型 | 提供商 | 特点 | 推荐场景 | 价格 |
|--------|--------|------|----------|------|
| **Gemini** | Google | 强大、快速 | 国际用户 | 免费 |
| **GPT-4** | OpenAI | 最强大 | 高精度需求 | 付费 |
| **Claude** | Anthropic | 安全专家 | 代码审计 | 付费 |
| **通义千问** | 阿里云 | 国内稳定 | 国内用户 | 免费+付费 |
| **文心一言** | 百度 | 中文优化 | 国内用户 | 免费+付费 |
| **智谱GLM** | 智谱AI | 开源友好 | 国内用户 | 免费+付费 |
| **DeepSeek** | DeepSeek | 高性价比 | 成本敏感 | 低价 |
| **Kimi** | 月之暗面 | 长文本 | 大文件分析 | 免费+付费 |
| **讯飞星火** | 科大讯飞 | 国产替代 | 政企用户 | 付费 |

---

## ⚙️ 配置方法

### 基础配置

在 `config.json` 中启用AI分析：

```json
{
    "ai_providers": {
        "enabled": true,                  // 总开关
        "primary_provider": "gemini",     // 主要使用的AI
        "fallback_enabled": true          // 启用备用切换
    }
}
```

---

## 🚀 各AI模型配置详解

### 1️⃣ Google Gemini（推荐 - 免费）

**优点**：
- ✅ 完全免费
- ✅ 性能强大
- ✅ 响应快速

**配置**：

```json
"gemini": {
    "enabled": true,
    "api_key": "YOUR_GEMINI_API_KEY",
    "model": "gemini-2.0-flash-exp"
}
```

**获取API Key**：
1. 访问：https://aistudio.google.com/app/apikey
2. 使用Google账号登录
3. 点击"Create API Key"
4. 复制密钥填入配置

**测试**：
```bash
python3 ai_analyzer.py test
```

---

### 2️⃣ OpenAI GPT-4

**优点**：
- ✅ 最强大的AI模型
- ✅ 代码理解能力强

**配置**：

```json
"openai": {
    "enabled": true,
    "api_key": "sk-proj-...",
    "model": "gpt-4-turbo-preview",
    "organization": ""
}
```

**获取API Key**：
1. 访问：https://platform.openai.com/api-keys
2. 创建API Key
3. 充值账户（按使用量计费）

**费用**：
- GPT-4-Turbo: $10/1M tokens (输入) + $30/1M tokens (输出)
- GPT-3.5-Turbo: $0.50/1M tokens (更便宜)

---

### 3️⃣ Anthropic Claude

**优点**：
- ✅ 安全性高
- ✅ 代码审计专业

**配置**：

```json
"claude": {
    "enabled": true,
    "api_key": "sk-ant-...",
    "model": "claude-3-opus-20240229"
}
```

**获取API Key**：
1. 访问：https://console.anthropic.com/
2. 创建API Key
3. 充值账户

**模型选择**：
- `claude-3-opus-20240229` - 最强大（$15/$75 per 1M tokens）
- `claude-3-sonnet-20240229` - 平衡（$3/$15 per 1M tokens）
- `claude-3-haiku-20240307` - 快速便宜（$0.25/$1.25 per 1M tokens）

---

### 4️⃣ 阿里通义千问

**优点**：
- ✅ 国内访问快
- ✅ 有免费额度

**配置**：

```json
"qianwen": {
    "enabled": true,
    "api_key": "sk-...",
    "model": "qwen-max"
}
```

**获取API Key**：
1. 访问：https://dashscope.console.aliyun.com/
2. 开通模型服务
3. 创建API Key

**模型选择**：
- `qwen-max` - 最强大
- `qwen-plus` - 平衡
- `qwen-turbo` - 快速便宜

---

### 5️⃣ 百度文心一言

**优点**：
- ✅ 中文优化好
- ✅ 有免费额度

**配置**：

```json
"wenxin": {
    "enabled": true,
    "api_key": "YOUR_API_KEY",
    "secret_key": "YOUR_SECRET_KEY",
    "model": "ERNIE-Bot-4"
}
```

**获取API Key**：
1. 访问：https://console.bce.baidu.com/qianfan/
2. 创建应用
3. 获取API Key和Secret Key

**模型选择**：
- `ERNIE-Bot-4` - 最强大
- `ERNIE-Bot` - 标准版
- `ERNIE-Bot-turbo` - 快速版

---

### 6️⃣ 智谱GLM

**优点**：
- ✅ 开源友好
- ✅ 性价比高

**配置**：

```json
"zhipu": {
    "enabled": true,
    "api_key": "YOUR_API_KEY.xxx",
    "model": "glm-4"
}
```

**获取API Key**：
1. 访问：https://open.bigmodel.cn/
2. 注册并实名认证
3. 创建API Key

---

### 7️⃣ DeepSeek

**优点**：
- ✅ 价格最便宜
- ✅ 代码理解强

**配置**：

```json
"deepseek": {
    "enabled": true,
    "api_key": "sk-...",
    "model": "deepseek-chat"
}
```

**获取API Key**：
1. 访问：https://platform.deepseek.com/
2. 注册并充值
3. 创建API Key

**价格**：约$0.14/$0.28 per 1M tokens（极低）

---

### 8️⃣ Kimi (月之暗面)

**优点**：
- ✅ 支持长文本
- ✅ 适合大文件

**配置**：

```json
"kimi": {
    "enabled": true,
    "api_key": "sk-...",
    "model": "moonshot-v1-8k"
}
```

**获取API Key**：
1. 访问：https://platform.moonshot.cn/
2. 注册并充值
3. 创建API Key

**模型选择**：
- `moonshot-v1-8k` - 8K上下文
- `moonshot-v1-32k` - 32K上下文
- `moonshot-v1-128k` - 128K上下文（超长文本）

---

### 9️⃣ 讯飞星火

**优点**：
- ✅ 国产可控
- ✅ 政企友好

**配置**：

```json
"xunfei": {
    "enabled": false,
    "app_id": "YOUR_APP_ID",
    "api_key": "YOUR_API_KEY",
    "api_secret": "YOUR_API_SECRET",
    "model": "generalv3.5"
}
```

⚠️ **注意**: 讯飞星火使用WebSocket协议，当前版本暂不支持，建议使用其他AI。

---

## 🔧 高级配置

### 多AI备用切换

启用备用切换后，如果主AI不可用，系统会自动尝试其他AI：

```json
{
    "ai_providers": {
        "enabled": true,
        "primary_provider": "gemini",     // 优先使用Gemini
        "fallback_enabled": true,         // 启用备用
        "gemini": {"enabled": true, ...},
        "openai": {"enabled": true, ...}, // 备用1
        "claude": {"enabled": true, ...}  // 备用2
    }
}
```

**切换顺序**：
1. 主AI (primary_provider)
2. 其他已启用的AI（按配置顺序）
3. 静态分析（最后备用）

---

## 🧪 测试AI配置

### 测试所有AI

```bash
cd ~/BTAUTOCHECK
python3 ai_analyzer.py test
```

输出示例：
```
======================================================================
🧪 测试所有AI提供商
======================================================================

📡 测试 GEMINI...
✅ gemini 可用 - 评分: 85

📡 测试 OPENAI...
❌ openai 不可用

⚪ claude 未启用
⚪ qianwen 未启用
...
```

### 测试单个文件分析

```python
from ai_analyzer import AIAnalyzer

analyzer = AIAnalyzer()
result = analyzer.analyze_code("""
def process(data):
    eval(data)
""", "test.py")

print(result)
```

---

## 💡 推荐配置方案

### 方案1: 免费方案（个人用户）

```json
{
    "ai_providers": {
        "enabled": true,
        "primary_provider": "gemini",
        "fallback_enabled": false,
        "gemini": {
            "enabled": true,
            "api_key": "YOUR_KEY",
            "model": "gemini-2.0-flash-exp"
        }
    }
}
```

**特点**：完全免费，适合个人使用

### 方案2: 双保险方案（推荐）

```json
{
    "ai_providers": {
        "enabled": true,
        "primary_provider": "gemini",
        "fallback_enabled": true,
        "gemini": {
            "enabled": true,
            "api_key": "YOUR_GEMINI_KEY",
            "model": "gemini-2.0-flash-exp"
        },
        "qianwen": {
            "enabled": true,
            "api_key": "YOUR_QIANWEN_KEY",
            "model": "qwen-max"
        }
    }
}
```

**特点**：国内外双备份，稳定性高

### 方案3: 专业方案（企业用户）

```json
{
    "ai_providers": {
        "enabled": true,
        "primary_provider": "claude",
        "fallback_enabled": true,
        "claude": {
            "enabled": true,
            "api_key": "YOUR_CLAUDE_KEY",
            "model": "claude-3-opus-20240229"
        },
        "openai": {
            "enabled": true,
            "api_key": "YOUR_OPENAI_KEY",
            "model": "gpt-4-turbo-preview"
        }
    }
}
```

**特点**：最强大，适合对安全要求极高的场景

---

## 🔄 分析流程

```
开始检测
    ↓
静态规则分析（3407个文件）
    ↓
选择高风险文件（~10个）
    ├── ajax.py
    ├── auth.py
    ├── plugin.py
    └── ...
    ↓
AI深度分析
    ├── 主AI（Gemini）
    │   ├─ 成功 → 返回结果
    │   └─ 失败 ↓
    ├── 备用AI1（OpenAI）
    │   ├─ 成功 → 返回结果
    │   └─ 失败 ↓
    └── 静态分析（兜底）
    ↓
综合评分 = 静态分析(70%) + AI分析(30%)
    ↓
生成报告
```

---

## 📊 AI vs 静态分析

| 对比项 | 静态规则分析 | AI深度分析 |
|--------|-------------|-----------|
| 速度 | ⚡ 极快 (5秒) | 🐌 较慢 (30-60秒) |
| 准确度 | 🎯 高（针对已知模式） | 🔍 更高（理解代码逻辑） |
| 误报率 | ⚠️ 中等 | ✅ 较低 |
| 成本 | 💰 免费 | 💳 可能收费 |
| 覆盖面 | 📋 规则库 | 🧠 智能推理 |

**推荐**：同时使用静态分析和AI分析，互补优势！

---

## 🎯 实际使用建议

### 国际用户
- **首选**: Gemini（免费且强大）
- **备用**: Claude（安全专家）

### 国内用户
- **首选**: 通义千问（稳定快速）
- **备用**: 文心一言（中文优化）

### 企业用户
- **首选**: Claude Opus（最专业）
- **备用**: GPT-4（全能）

### 成本敏感
- **首选**: DeepSeek（最便宜）
- **备用**: Gemini（免费）

---

## ❓ 常见问题

### Q1: AI分析会增加多少时间？

**A**: 约30-60秒（分析10个文件）。总检测时间从5秒变为35-65秒。

### Q2: 可以禁用AI分析吗？

**A**: 可以！设置 `"ai_providers": {"enabled": false}`，系统会仅使用静态分析。

### Q3: 多个AI都配置会同时调用吗？

**A**: 不会。系统会按优先级依次尝试，只使用第一个成功的AI。

### Q4: AI分析会泄露代码吗？

**A**: 代码会发送给AI提供商进行分析。如果担心隐私，建议：
- 使用私有化部署的AI（如本地LLaMA）
- 或仅使用静态分析

### Q5: 如何选择最合适的AI？

**A**: 根据您的需求：
- **免费**: Gemini
- **国内**: 通义千问 / 文心一言
- **专业**: Claude Opus
- **便宜**: DeepSeek

---

## 📈 性能优化

### 1. 只分析关键文件

系统默认只分析高风险文件（ajax, api, auth, login, admin, plugin），而不是全部文件。

### 2. 限制分析数量

配置中 `max_files=10` 限制了最多分析10个文件，避免API费用过高。

### 3. API限流保护

每次AI调用后等待1秒，避免触发限流。

---

## 🔐 安全说明

### API Key保护

- ✅ 使用加密存储（`secure_config.py`）
- ✅ 配置文件已加入 `.gitignore`
- ✅ Web界面密码保护

### 推荐做法

```bash
# 1. 加密API Key
python3 secure_config.py encrypt

# 2. 验证加密
grep "api_key" config.json
# 应该显示: "api_key": "ENC[...]"

# 3. 设置文件权限
chmod 600 config.json
chmod 600 .config.key
```

---

**配置完成后，运行完整检测：**

```bash
python3 auto_update.py
```

系统会自动进行：
1. ✅ 静态规则分析（3407个文件）
2. ✅ AI深度分析（10个高风险文件）
3. ✅ 生成综合报告

---

**🎉 AI安全分析配置完成！**

